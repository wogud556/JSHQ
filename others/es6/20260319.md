## 모르는 부분만 정리

### 삼항연산자
- if 조건식 2개 사용할떄
- ex
```
console.log(idDiscounteds ? : "$10" : "$20");
```

### 템플릿에 리터럴 함수 추가
- 필요시 템플릿 리터럴 내에 함수를 전달할 수 있음
- 예제
```
function groceryList(others) {
    return `
        <p>
            ${others.map( other => `<span>${other}</span>`).join('\n')}
        </p>
    `
}

const markup = `
    <div>
        ${groceryList(groceries.others)}
    </div>
`
```

### 디스트럭처링(리액트)
- 객체 변수 선언 시 기존에는 아래와 같이 사용
```
var person = {
    first : "a",
    last : "b"
};

var first = person.first;
var last = person.last;
```
- ES6에서는 아래와 같이 간결하게 사용
```
const person = {
    first : "a",
    last : "b",
};
const {first, last} = person;
```

### 객체에 대한 반복
- Object.keys()를 사용하여 객체의 모든 키를 가져온 후 키에 대해 반복을 수행하면서 값에 접근하는 겻이 가능함
- ex
```
const car = {
    maker: "BMW",
    color: "red",
    year: "2010"
}
for (const prop of Object.keys(car)) {
    const value = car[prop];
    console.log(prop, value);
}

``` 

### for in loop
- for와 in을 사용하여 주로 배열을 꺼내올때 사용함

### 스프레드 문법
- 0개 이상의 인수 또는 원소가 예상되는 위치에서 배열 표현식 또는 문자여로가 같은 이터리블 항목을 확장하거나 0개 잇아의 키/값 쌍이 예상되는 위치에서 객체 표현식을 확장함
- 예시
```
const veggie = ["tomato", "cucumber", "beans"]
const meat = ["pork","beef","chicken"];

const menu = [...veggie, "pasta", ...meat]
```

### 배열의 복사
- 스프레드 문법은 배열의 복사본 생성시 유리함
- 아래와 같이 이용하여 배열을 생성한다.
```
const veggie = ["tomato", "cucumber","beans", "peas"]
const newVeggie = [...veggie];


```

### 레스트 매개변수

### 심벌
- 객체 속성의 식별자로 사용함
- 아래와 같은 코드로 심벌을 생성함
```
const me = Symbol("Alberto")
console.log(me)
```

### 객체 속성에 대한 식별자

## 프로미스
- 자바스크립트는 동기적으로 작동함
- 이전 블록 이후에 실행된다는 소리

```
const data = fetch('your-api-url-goes-here');
console.log('Finished');
console.log(data);
```
- 동기 코드의 경우 우리는 fetch를 사용해서 어떤 URL에서 데이터를 가져옴(이해를 돕기 위해 그러는 것)
- 동기 코드의 경우 우리는 fetch 작업이 실제로 완료된 후에 다음 행이 호출되리라고 예상함
- 하지만 실제로 fetch 가 호출된 직후 바로 다음 행에 있는 두 console.log(data)는 undefined를 출력함

### 콜백지옥
- 비동기코드를 동기식으로 작성하는 것처럼 하기 위해 콜백으로 여러 코드 블록을 차례로 연결해 작성할 때 발생하는 상황을 콜백지옥이라고 부름
- a를 하고 a가 끝날때까지 기다렸다 b를수행하고 b끝날때까지 기다렸다 c를 보고
```
const makePizza = (ingredients, callback) => {
    mixIngredients(ingredients, function(mixedINgredients)) {
        bakePizza(mixedIngredients, function(backedPizza)) {
            console.log('finished');
        }
    }
};
```
- 이렇게 하면 시각적으로 위에서 아래로 코드가 실행되는 것처럼 보이게 작성할 수는 있지만, 그것 때문에 과도한 함수 중첩을 유발하고 맘
- 콜백지옥을 개선하는 방법은 
- 프로미스를 사용하면 이 지옥에서 벗어날 수 있음

### 프로미스
- 비동기 작업의 최종 성공 또는 실패를 나타내는 객체
```
const myPromise = new Promise((resolve, reject) => {
    //여기에 코드작성
})
```
- 이런 방식으로 프로미스를 만든 후 프로미스의 성공을 알리기 위해서는 reslove를 실패를 알리기 위해서는 reject를 호출하면 됨
```
const myPromise = new Promise((resolve, reject) => {
    resolve("The value get from the promise")
})

myPromise.than(
    data => {
        conesole.log(data);
    }
);
```
- resolve 함수의 첫번째 매개변수 전달된 값이 콘솔에 출력되는 것을 확인할 수 있음
- setTimeout() 을 이용하면 resolve가 호출되기 전까지 일정 시간을 기다릴수 있음
```
const myPromise = new Promise((resolve, reject) => {
    setTimeoout(() => {
        resolve("the value we get from the promise");
    }, 2000);
});

myPromise.then(
    data => {
        console.log(data);

    }
);
```
- 이 두가지 예는 매우 간단한 예에 불과하지만, 프로미스는 많은 비동기코드를 수행할 때 매우 유용하다.
- 이 예에서 간단하게 resolve를 호출하여 프로미스가 성공하는 경우만 살펴봤지만 실제로는 오류도 발생하기때문에, reject를 이용한 오류처리방법도 살펴본다
```
const myPromise = new Promise((resolve, reject) => {
    setTimeout(() => {
        reject(new Error("this is our error"));
    }, 2000);
})

myPromise
    .then(data => {
        console.log(data);
    })
    .catch(err => {
        console.log(data);
    });

```
- 프로미스가 성공할 때의 값을 얻는 데에 .then() 을 사용하고, 프로미스가 실패 할 때의 오류를 처리하는 데에는 .catch()를 사용함
- 출력된 오류 로그를 보면 오류가 발생한 위치를 알 수 있음.
- 단순히("this is our error"); 라고 작성하지 않고 reject(new Error("this is our error")); 라고 작성했기 때문
- 프로미스의 성공 또는 실패 여부와 무관하게 이전 프로미스에서 반환된 것을 후속 프로미스의 기반으로 사용하여 프로미스를 계속 체이닝(연결) 할 수 있음
- 원하는 만큼 많은 프로미스를 연결할 수 있으며, 그 코드는 위에서 봤던 콜백 지옥의 코드보다 읽고 쉽고 간결함
```
const myPromise = new Promise((resolve, reject) => {
    reslove();
})

myPromise
    .then(data => {
        // 새로운 값을 반환
        return 'working...';
    })
    .then(data => {
        console.log(data);
        throw 'failed';
    })
    .catch(err => {
        //프로미스 수행 중 발생한 오류를 받아서 출력
        console.error(err);
        // failed
    });

const myPromise = new Promise((resolve, reject) => {
    resolve();
});

myPromise
    .then(data => {
        throw new Error("oooops");
        console.log("first value");
    })
    .catch(() => {
        console.log("catched an error");
    })
    .then(data => {
        console.log("second value");
    });
    // catched an error
    // second value

```
- 이 코드의 경우 첫 번째 .then()에서 오류가 발생했기 때문에 "first value"는 출력되지 않고, 첫 번째 .catch()와 마지막. then()을 수행하면서 로그가 출력됨

### promise.reslove() 와 promise.reject()
- promise.resolve()와 promise.reject()는 자동으로(즉시)성공하거나 실패하는 프로미스를 생성함
```
//Promise.resolve()
Promise.resolve('Success').then(function(value) {
    console.log('Success');
    // Success
}, function (value) {
    console.log('fail')
});

// Promise.reject()
Promise.reject(new Error('fail')).then(function() {
    //not called
}, function(error){
    console.log(error);
});
```
- 첫 번째 예제에서 볼 수 있듯이 .then()절에서 생성된 프로미스에는 두개의 인수가 있음
- 하나는 프로미스가 성공할 때 호출되는 함수, 하나는 프로미스가 실패할 때 호출되는 함수임
- Promise.resolve()는 즉시 프로미스를 성공처리하므로 첫 번째 함수가 호출됨
- 한편, 두 번째 에제에서는 Promise.reject()를 사용하여 프로미스를 즉시 실패 처리하므로 .then() 절의 두 번째 인수가 호출됨

### Promise.all()과 Promise.race()
- Promise.all()은 모든 프로미스가 성공할 경우에만 성공하는 하나의 프로미스를 반환함
- 두가지 프로미스가 있는 다음 예제를 살펴봄
```
const promise1 = new Promise((resolve, reject) => {
    setTimeout(resolve, 500, 'first value');
});
const promise2 = new Promise((resolve, reject) => {
    setTimeout(resolve, 1000, 'second value');
});

promise1.then(data => {
    console.log(data);
});
// 500ms후
// first value
```
- 여기서 각 프로미스는 서로 독립적으로 성공처리됨
- Promise.all()을 사용한다면 어떤 일이 발생하든지 다음 예제에서 살펴봄

```
const promise1 = new Promise((resolve, reject) => {
    setTimeout(resolve, 500, 'first value');
})

const promise2 = new Promise((resolve, reject)=> {
    setTimeoout(resolve, 1000, 'second value');
})

Promise
    .all([promise1, promise2])
    .then(data => {
        const[promise1data, promise2data] = data;
        console.log(promise1data, promise2data);
    });
// 1000ms
// first value second value
```
- 1000ms (두 번째 프로미스의 타임아웃) 후에 첫 번째, 두 번째 프로미스 결과가 함께 반환되었음.
- 즉 첫 번째 프로미스가 성공 후에도 두 번째 프로미스가 성공할 떄까지 기다렸음을 알 수 있음
- 비어 있는 이터러블을 전달하면 이미 성공처리된 프로미스를 반환함
- 프로미스 중 하나가 실패하면 다른 모든 프로미스가 성공하더라도 해당 실패에서 발생한 오류가 반환됨
```
const promise1 = new Promise((resolve, reject) => {
    resolve("my first value");
});
const promise2 = new Promise((resolve, reject) => {
    reject(Error("oooops error"));
})

Promise
    .all([promise1, promise2])
    .then(data => {
        const [promise1data, promise2data] = data;
        console.log(promise1data, promise2data);
    })
    .catch(err => {
        console.log(err);
    });
    // Error
```
- 이와 대조적으로 Promise.race()는 이터러블에 포함된 프로미스를 중 가장 먼저 성공 또는 실패한 결과를 반환함

```
const promise1 = new Promise((resolve, reject)=> {
    setTimeout(resolve, 500, 'first value');
});
const promise2 = new Promise((resolve, reject) => {
    setTimeout(resolve, 100, 'second value');
});

Promise.rece([promise1, promise2]).then(function(value) {
    console.log(value);
    // 둘 다 성공하지만 promise2가 더 빨리성공
});
// second value
```
- 비어 있는 이터러블을 전달하면 .race()는 영원히 보류된 상태로 남아 있음에 주의한다.

### 제네레이터
- 제네레이터 함수는 원하는 만큼 코드 실행을 시작하거나 중지할 수 있는 함수
- 중지된 제네레이터 함수를 다시 시작할 때 데이터를 추가로 전달하면서 재시작할 수 있음
- 제네레이터 함수를 생성하려면 다음과 같이 코드를 작성함

```
function * fruitList() {
    yield 'Banana';
    yield 'Apple';
    yield 'Orance';
}

const fruits = fruitList();

fruits.next();
// {value: "Banana", done: false}
fruits.next();
// {value: "Apple", done: false}
fruits.next();
// {value: "Orange", done: false}
fruit.next();
// {value: undefined, done: false}
```
- 코드를 하나씩 살펴본다
  - function* 을 사용하여 함수를 선언함
  - 반환할 콘텐츠 앞에 yeild 키워드를 사용함
  - .next()를 사용하여 함수의 실행을 시작함
  - 마지막으로 .next()를 호출하면 undefied값과 done:true가 반환됨
- 이 예시의 함수는 각 .next()호출 사이에서 일시 중지된 상태에 있음

### 제네레이터를 사용하여 배열 복사하기
- for of 루프를 사용하면 제너레이터에 대해 반복하고 각 루프에서 콘텐츠를 반환(yield) 할 수 있음
```
// 과일 배열을 생성
const fruitList = ['Banana', 'Apple','Orange','Melon','Cherry','Mango'];

// 제네레이터를 생성
function* loop(arr){
    for(const item of arr) {
        yield `I Like to eat ${item}s`;
    }
}

const fruitGenerator = loop(fruitList);
fruitGenerator.next();
// {value: "I like to eat Bananas", done : false}
fruitGenerator.next();
// {value: "I like to eat Apples",done: false}
fruitGenerator.next().value;
// "I like to eat Oranges"
```
- 새로운 제너레이터는 배열을 반복하고 .next()를 호출할 때마다 한 번에 하나의 값을 출력함
- 값을 가져오는 것에 대해서만 관심이 있다면 .next().value를 사용하면 됨
- 이렇게 하면 제너레이터의 상태는 출력하지 않음

### .return()을 사용하여 제너레이터 종료
- .return()을 사용하여 주어진 값을 반환하고 제너레이터를 종료할 수 있음
```
function * fruitList() {
    yield 'Banana';
    yield 'Apple';
    yield 'Orange';

}

const fruits = fruitList();

fruit.return();
// {value: undefied, done: true}
```
- 이 경우 .return()에 아무것도 전달히자 않았기 때문에 value: undefied를 얻음

### .throw()로 오류잡기
```
function * gen(){
    try {
        yield "Trying...";
        yield "Trying harder...";
        yield "Trying even harder..";
    }
    catch(err){
        console.log("Error: " + err);
    }
}

const myGenerator = gen();
myGenerator.next();
// {value: "Trying...", done: false}
myGenerator.throw("oooops");
// Error: oooops
// {value: undefied, done: true}
```
- 보다시피 .throw()를 호출했을 때 제네레이터는 오류를 반환했고, 실행할 수 있는 yield가 하나 더 남아 있는데도 종료되었음

### 제네레이터와 프로미스를 같이 사용하기
- 앞서 살펴본 것처럼 프로미스는 비동기 프로그래밍에 매우 유용함
- 제네레이터와 같이 사용하면 콜백 지욕과 같은 문제를 방지할 수 있는 매우 강력한 도구임
- 여기에서는 ES6에 대해서만 논의하고 있으므로 ES2017에서 도입된 비동기 (async)함수에 대해 설명하지는 않는다
- 단지 비동기 작동 방식이 여기서 다룰 내용을 기반으로 한다는 것 정도만 알고 있으면 됨
- 비동기 함수에 대해서는 19장에서 더 자세히 살펴볼 것
- 제너레이터를 프로미스와 함꼐 사용하면 마치 동기코드처럼 느껴지게 비동기 코드를 작성할 수 있음
- 프로미스가 완료될 때까지 기다려싸가 완료될 때까지 반환된 값을 .next()호출 시점에 제너레이터로 다시 전달하는 다음 코드를 살펴봄
```
const myPromise = () => new Promise((resolve)=> {
    resolve("our value is...");
});

function* get(){
    let result = "";
    // 프로미스를 반환
    yeild myPromise().then(data => {result = data});
    // 프로미스의 결과를 기다린 후 이 값을 사용
    yield result + ' 2';
};

// 비동기 함수를 호출
const asyncFunc = gen();

const val1 = asyncFunc.next();
console.log(val1);

// {value: Promise, done: false}
// 프로미스가 완료되기를 기다린 후 .next()를 호출
val1.value.then(()=> {
    console.log(asyncFunc.next());
});
// {value: "our value is ... 2", done: false}
```
- .next()를 처음 호출하면 프로미스를 반환함.
- 해당 프로미스가 완료되기를 기다렸다가 .next()를 다시 호출하면 제너레이터 내부에서는 프로미스에서 반환한 값을 사용하여 작업을 수행함(이 예제에서는 반환된 값에 문자열을 이어 붙임)